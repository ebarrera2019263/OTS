import { CategoryClickFilterPipe } from './category-click-filter.pipe';

describe('CategoryClickFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryClickFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
